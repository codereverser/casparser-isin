[tool.black]
line-length = 100
target-version = ['py38']

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--cov=casparser_isin --cov-config=setup.cfg --cov-report=xml --cov-report=html"
testpaths = [
    "tests",
]